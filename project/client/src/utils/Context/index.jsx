import { /* React, useState, */ createContext } from "react";

export const UidContext = createContext(); // Je crée le context

// export const UidProvider = ({ children }) => {
  // Je crée le composant Provider

  // const [uid, setUid] = useState();
  // setUid("Luther");

  // return <UidContext.Provider value={{ uid }}>{children}</UidContext.Provider>;
// };

// export default UidProvider;
